- hosts: localhost
  gather_facts: no
  vars:
    foo_null:
  tasks:
    - name: Grab value from templating to check its none
      debug:
        msg: "{{ foo_null }}"
      register: foo_null_out
    - name: Verify default value is None
      assert:
        that:
          - foo_null == None

