- hosts: localhost
  gather_facts: no
  vars:
    foo_null:
  tasks:
    - name: print value to check its null
      debug:
        msg: "{{ foo_null }}"
      register: foo_null_out
    - name: Verify null value is an empty string
    - assert:
        that:
          - foo_null_out.msg == ""

            